const app = '解谜功能'
const appType = '插件'
const appTypeId = 0
const appName = `${app}${appType}文档`
const appDescription = `${app}${appType}文档`

export {
  app,
  appName,
  appType,
  appTypeId,
  appDescription,
}
